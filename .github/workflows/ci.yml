name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  self-check:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run TOI self-check
        shell: pwsh
        run: ./tests/self-check.ps1

      - name: Capture TOI self-check JSON
        shell: pwsh
        run: ./tests/self-check.ps1 -Json | Out-File -FilePath toi-self-check.json -Encoding utf8

      - name: Capture TOI workflow report
        shell: pwsh
        run: ./toi.ps1 report | Out-File -FilePath toi-report.md -Encoding utf8

      - name: Capture TOI workflow report JSON
        shell: pwsh
        run: ./toi.ps1 report -Json | Out-File -FilePath toi-report.json -Encoding utf8

      - name: Capture TOI schema JSON
        shell: pwsh
        run: ./toi.ps1 schema -Json -Snapshot | Out-File -FilePath toi-schema.json -Encoding utf8

      - name: Publish job summary
        shell: pwsh
        run: |
          Get-Content -Raw -Path ./toi-report.md | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append

      - name: Upload TOI self-check artifact
        uses: actions/upload-artifact@v4
        with:
          name: toi-self-check
          path: toi-self-check.json

      - name: Upload TOI workflow report artifact
        uses: actions/upload-artifact@v4
        with:
          name: toi-workflow-report
          path: |
            toi-report.md
            toi-report.json

      - name: Upload TOI schema artifact
        uses: actions/upload-artifact@v4
        with:
          name: toi-schema
          path: toi-schema.json
