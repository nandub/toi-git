name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  self-check:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run TOI self-check
        shell: pwsh
        run: ./tests/self-check.ps1
